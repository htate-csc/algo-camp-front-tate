server {
  listen 80;

  location / {
    root /usr/share/nginx/html;
    index index.html index.htm;
    try_files $uri /index.html =404;
  }

  location /api-paiza/ {
    proxy_pass https://api.paiza.io/;
    proxy_set_header Host api.paiza.io;
  }

  include /etc/nginx/extra-conf.d/*.conf;
}
